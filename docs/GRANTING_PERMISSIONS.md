# HEXPal — No Permissions Required

HEXPal requires **zero system permissions**. No setup is needed.

- **Color picking**: Uses Apple's native `NSColorSampler` API — no entitlements or privacy permissions
- **Global hotkey**: Uses the KeyboardShortcuts package (Carbon APIs) — no permissions required

The app launches and works immediately with no configuration.
